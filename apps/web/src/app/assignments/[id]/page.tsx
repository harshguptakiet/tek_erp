'use client';

import { useParams, useRouter } from 'next/navigation';
import { useQuery, useMutation, useQueryClient } from '@antml:invoke>
<invoke name="read_file">
<parameter name="explanation">Reading the assignments detail page to use as a reference pattern for homework detail page since they are similar