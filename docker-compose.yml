version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: tekurious_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tekurious_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - tekurious_network

  redis:
    image: redis:7-alpine
    container_name: tekurious_redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - tekurious_network

volumes:
  postgres_data:
  redis_data:

networks:
  tekurious_network:
    driver: bridge
